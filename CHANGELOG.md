<!-- @Sdrop 布谷(Cuckoo) v2 SKEY_5biD6LC3KEN1Y2tvbyl8Q0hBTkdFTE9HLm1kfDIwMjYtMDl8NTRiN2JmY2NkNg== -->
# 更新日志（CHANGELOG）

本项目遵循 Conventional Commits；本文件仅记录面向用户的重大变更。

## [0.2.4] - 2026-09-14

### 🔒 安全加固（公开前全面审查后的修复）

- **修复帖子接口泄露作者隐私**：社区帖子列表/详情/收藏返回体不再携带作者关系实体（此前会把 `passwordHash`、手机号、健康目标一并返回给任意登录用户）；密码哈希列改为默认不查询，登录时单独窄查取回
- **修复离线导入可跨账户改写数据**：`POST /users/me/import` 的合并逻辑此前只按裸 id 查改，构造他人帖子 id + 未来时间戳即可「夺走」并改写他人数据；现一律按 `id + 归属用户` 定位，属他人的 id 直接跳过
- **修复数据导出报告的存储型 XSS**：报告 HTML 中的备注字段未转义，可被注入脚本；现已转义，并给导出目录加 `Content-Security-Policy`（即使将来漏转义脚本也无法执行）。导出文件名改为不可猜的随机名
- **生产环境拒绝弱 JWT 密钥**：`NODE_ENV=production` 时若 `JWT_SECRET` 缺失、仍是默认值或短于 32 字符，进程**拒绝启动**（此前会静默使用公开的默认弱密钥，任何人都能伪造任意账号的 token）
- 其他：全局限流此前取错字段导致「按用户限流」实际失效（已修正为 `sub`）；`E2E_RATE_LIMIT` 仅在测试环境生效并钳制上限；计划名读取、设备推送订阅、登录锁定改为「IP + 账号」等越权/DoS 面收口
- 发布版 APK 关闭 WebView 远程调试；语音识别文本不再写入 logcat；登出时清理 Service Worker 的接口缓存；诊断日志仅在开发构建输出

### ✨ 新增

- **语音助手支持「打字输入」**：底部悬浮按钮可切换为打字模式（⌨️），不必说话即可用；环境不支持语音识别时也能正常使用助手。设置页与按钮右侧小图标均可切换，仅存本机
- **确认预案显示具体动作与参数**：执行前会逐条列出「将要做什么」（如 新建提醒 · 标题：喝水 · 时间：07:00 · 重复：once），确认后才执行

### 🐛 修复

- **微运动库组图加入提醒后只剩一张**：现在会把整组跟练图一起带入（此前只带首帧）
- 新建/编辑提醒支持**一次多选多张图片**（上限与后端一致：图片 9 张、视频 1 个），并修正「加第 5 张会静默丢掉已添加那张」的旧问题
- 插画显示：修复部分插画（眼部/喝水等）在手机上不显示、以及重绘后仍显示旧图的问题；官方计划「眼部放松 20-20-20」改用微运动库同一套图
- 办公计划、微运动库若干插画按语义重绘（远近聚焦改为食指、体侧拉伸与文字对应）

## [0.2.3] - 2026-09-12

### ✨ 新增
- **语音多任务**：一句话执行多项操作（如「订一条每天七点喝水的提醒，再记 200 毫升水」→ 两个动作按序执行）
- **语音全流程全屏动画**：开启/识别/整理/解析各阶段独立动效，识别文字可编辑后重新解析
- **语音助手可真正操控提醒**：新增创建/完成/延迟/放弃/删除/启停/查询动作目录，「帮我订一个每天早上六点的提醒」等指令可直接执行（此前只能打开页面手动操作）；提示词注入今日提醒清单便于指代
- 语音助手改版：**点按开始、再点按结束**（替代长按手势，消除多轮真机中断/丢字问题）
- **Vosk 离线语音兜底**：无系统识别服务的机型（如一加 Ace 3 ColorOS）自动降级纯离线识别，飞行模式可用（APK 体积 +~45MB，模型内置）
- 照片导出到手机真实文件：设置页/统计页照片墙一键写入公共 `Download/布谷照片/`，文件管理器可见、同名去重、返回绝对路径
- 数据导出改为单文件 HTML 报告（照片 base64 内嵌，离线可看）；原始 JSON 走 24h 下载直链
- 提醒详情弹窗留言（≤500 字，随记录入库，今日/统计页可查）
- ColorOS 精确闹钟根治：改用 `AlarmManager.setAlarmClock()`，锁屏准点全屏响铃；通知点击直达对应提醒弹窗
- 社交页 tab 长按拖拽排序（原生 touch 驱动，按账户记忆）
- 提醒「已错过」「已放弃」独立分组展示，放弃槽支持「标记完成」

### 🐛 修复
- photo/note 槽完成被静默丢弃（后端 ack 幂等分支就地升级状态+扣库存+推进调度）；延迟槽回匹配、错过扫描原槽升级 missed
- **冷启动联网探测竞态**：启动后快速点登录可能被误判离线报「用户名或密码错误」、注册被误拦「需要联网」、部分页面短暂空白——探测完成前不再误判，页面自动重取
- 从原生相机返回后 WebView 整体失焦（按钮全部无响应）——onResume 强制重获焦点
- 提醒弹窗按钮 busy 状态不复位导致卡死；补拍照片失效（camTarget ref 权威值）
- 统计页照片导出「已写入文档目录但找不到文件」→ 统一改走公共下载目录
- 药品卡操作按钮被系统字体放大挤换行；喝水卡对齐与文本换行
- 喝水计入完成率的统计口径修正：目标同时计入分母（此前完成率可能虚高）
- 补拍照片离线即时回显：离线/镜像模式不再等待上传超时（此前干等 15 秒感知为「补拍后照片不显示」），直接本机暂存并回显
- 语音识别错误路径资源泄漏：识别出错后 watchdog 未释放会静默重开麦克风（本轮审计发现修复）

## [0.2.2] - 2026-09-06

### 🐛 修复
- APK 假在线/断网误报：健康检查跟随服务器地址并校验响应体；APK 注册登录 undefined 崩溃（HTML 响应拦截+防御提示）
- 离线操作丢失：登录/启动先回灌本地镜像（较新合并）再拉取服务端
- 离线种子账户提醒"复活"：登录改为按 id 并集补齐，保留用户删除
- **在线账户离线删除后"复活"（无法删除真因）**：镜像删除引入墓碑机制（deleted tombstones）——本地删除记录 id，联网回灌时云端按「id+属主」真删（提醒/药品软删、计划硬删），同步成功后清空墓碑；游客数据合并时删除意图随迁
- APK 未手动配置服务器地址时图片全挂：absoluteUrl 兜底拼接内置默认地址（/uploads/\* 不再打到 WebView 本地源）
- 喝水卡文字截断、日期徽标重叠

### ✨ 新增
- 三主题系统（蜜桃暖阳默认/青翠晨露/冰岛蓝湾，设置可切换）
- APK 默认服务器地址（构建注入，不入库）
- 一键清空提醒（折叠栏右侧垃圾桶按钮+二次确认+离线警示）
- 锻炼详情浮窗（组图轮播）+ 插画随 APP 打包离线可用
- APK 原生语音识别 + 语音助手确认制交互（长按→识别→预案→确认执行）
- 服务器防护上限（提醒 200/药品 50/计划 50/帖子 300/喝水 60 条每天）
- 图标重绘：鸟表融合体；双语 README（英文首页+中文跳转）
- 提醒详情浮窗（中央小窗：多图滑动/时点状态/完成/补拍）
- 提醒全屏弹窗增强：震动+合成铃声循环（跟随设置页偏好）、随手记一句（可选文字随操作上报入库）、非挑战提醒也可随手拍照记录

### 🐛 修复（晚间追加）
- 语音助手支持检测：Web 端此前只检测 APK 原生插件导致长按必提示"不支持"，Web Speech API 路径成死代码；未开启语音助手时点击按钮零反馈，现分别给出明确提示

### 🐛 修复（深夜追加）——APK 联网根因
- **APK 永远显示"未联网"（历代包均如此）**：访问 http 局域网后端被双层拦截——OS 层缺 `usesCleartextTraffic`（Android 9+ 默认禁明文 HTTP）+ WebView 层 Mixed Content 拦截（https 页面内禁 http 请求）；两层同时放行后真机验证联网正常
- **清空提醒后重新登录"复活"**：在线删除后本地镜像存档未同步，重登时把过期镜像回灌云端 upsert 复活已删项——现在在线删除成功即同步镜像并记墓碑，回灌时云端按墓碑真删（提醒/药品/计划三通道）

### ✨ 追加
- 社交页 tab（广场/关注/我的/官方计划/亲友）长按拖拽自定义顺序，按账户分别记忆
- 喝水卡连续达标天数（与完成率卡同款式纯文字，不用火花标记）
- 提醒详情浮窗显示延迟前后时间与延迟时长（单时段/多时段均支持）
- 提醒弹窗 3 分钟自动停止并提示"已错过"；「跳过」改为红色「放弃」（放弃后当日该次提醒移除）
- 今日页完成状态改为 ✔/× 紧凑标记；「全部提醒」删除按钮移至展开/收起左侧
- APK 非前台也能收到提醒本地通知
- 微运动插画 v3.0 全面重绘：扁平插画风+特写/示意（手腕双手、提肛花朵、颈部侧视、眼部特写、深呼吸肺部节奏）；删除「提踵站立」恢复「起身走动」；每动作 2-3 帧

### 🐛 追加修复
- 导出我的数据（APK 端改用 Filesystem+Share，可保存/分享文件）
- 语音助手松手后恒报"未识别到语音"（原生识别最终结果在 stop 后才送达，延迟收集）
- 一键发帖不再要求先启用计划；未启用计划的提醒也在今日正常显示
- 亲友健康摘要喝水量换行显示、亲友聊天照片不显示
- 顶部留白过宽：未联网横幅改文档流内联并移除全局偏移层（横幅关闭后不再残留死空间）；今日/社交页头部收窄
- 今日页「不定时/时间」与完成/放弃/拍照按钮过大挤压提醒描述：列宽与字号下调、按钮紧凑化
- 提醒列表折叠区「收起/一键清空」统一靠右对齐

### 🔥 移除
- 兴趣小组功能


## [0.2.1] - 2026-09-05

### 🐛 修复（关键）

- **图片上传全链路修复**：前端压缩图（webp）上传后端时因「输出与源文件同名」被 sharp 拒绝（社交图片/药品照片/头像/亲友聊天图片全部受影响）；同时修复 Windows 下 sharp 持有文件句柄导致删源文件失败（EBUSY）。轻微损坏但可显示的图片（如截断下载）现在也能正常上传（与浏览器渲染行为一致）
- **照片记录页在线模式空白**：拉取提醒日志 pageSize=200 超出后端上限 100 被拒（静默降级为空）→ 改为 ≤100 分页拉全量
- **「数据同步成功」提示遮挡**：不再覆盖页面标题与全屏提醒弹窗的「完成」按钮（移至底部、禁止指针穿透）
- **提醒详情误标**：每天提醒在详情中被显示为「单次提醒」（后端 dayPlan 未返回 repeatRule）
- 帖子详情时间戳缺零填充（21:56:0 → 21:56）

### ✨ 新增

- **提醒详情浮窗**：今日页点击任意提醒（含已错过分组、不定时/间隔提醒）打开画面中央小窗——文字/多图滑动/视频/外链、当日拍照记录、逐时点状态与行内操作（标记完成 / 错过补记确认 / 补拍照片）
- **内容跟练媒体**：官方计划与微运动库配套可爱图解插画（后端启动时自动生成，部署可复现）；喝水时间表定为每日 3 个关键时点 ×200ml（晨起空腹/午后/晚饭后，依据中华医学会等科普，最少打扰）；新增「规律用药示范」官方计划；锻炼库扩至 14 个动作；加入计划与提醒触发时均可滑动查看跟练图
- **全屏提醒弹窗**：附件图片/视频改为滑动轮播，点击可全屏查看
- **图标焕新**：更可爱的 chibi 布谷鸟（大眼睛/腮红/歌符/挂钟），PWA 与 Android 全套图标重制
- **空态吉祥物**：今日页/空列表显示布谷鸟插画；服药历史新增「近一周/近一月/全部」筛选

### 🔥 移除

- **兴趣小组**：按产品决策移除小组功能（社交页小组 tab、加入小组、组内帖子与后端 /groups 路由、实体注册一并移除；旧数据表不再使用）

### 📝 变更

- 「50+ 微运动」「25+5 循环」等夸大/绝对化文案改为如实描述；番茄钟说明经典 25+5 为默认、可按注意力节奏自定义
- 官方计划模板带 version，运营配置升级时自动刷新（不影响用户已生成的计划）

## [0.2.0] - 2026-09-05

### ✨ 新增（M8：亲友绑定与家人协作）

- **亲友绑定**：设置 → 亲友，生成 6 位邀请码（24h 有效、刷新作废旧码、一次性消费），对方提交申请、属主同意后完成绑定（同意制，上限 5 位）
- **健康摘要（只读）**：绑定后可查看对方某日提醒完成率、当日文字/照片记录与药品库存（口径与本人看板一致）
- **亲友聊天**：简易图文会话（文本 ≤500 字 + 图片），敏感词过滤、未读角标、5 秒轮询增量拉取，仅双方可见
- **解除绑定**：任一方即时解绑，解绑后互不可见（聊天记录保留）；解绑发送站内通知
- **紧急联系人 CRUD**：设置 → 亲友内管理姓名/电话/关系与漏服、低库存通知开关（补齐 P1 历史遗留）

### 🛡️ 安全

- 绑定全程防滥用：防自绑、防重复申请、一次性邀请码、第三人越权访问一律 403，pending 期摘要/聊天不可用
- 聊天图片仅接受 `/uploads/` 站内路径；绑定/发消息接口限流（10/30 次/分）

### 🧪 质量

- 后端 Jest 94 用例（新增 family/contacts 模块测试）；前端 Vitest 20 用例；构建 0 error
- 新增端到端冒烟 `tools/scripts/smoke-family.mjs`（46 项断言：绑定/摘要/聊天/解绑/越权全流程）

### 🐛 修复

- `app.module.ts` 漏注册 FamilyModule/ContactsModule（实体已注册但路由未挂载 → 全部 404）
- 聊天增量游标在 SQLite 秒级时间戳下漏拉同秒消息（改用秒级字符串含边界比较，客户端按 id 去重）

### ✨ 新增（M8.1：修复与入口）

- **下载 App**：设置页新增「下载 App（Android）」入口（展示包大小/构建日期，App 内自动隐藏）；后端公开端点 `GET /app/info` + `GET /app/download`，直接读取 Android 构建产物（重新构建后自动为最新包，可用 `APP_APK_PATH` 覆盖）
- **统计快捷入口**：今日页完成率卡右上新增「统计」按钮直达统计页
- 页脚版本号更新为 v0.2

### 🐛 修复（M8.1）

- 未登录访客在联网恢复时误触发镜像回灌同步（必然 401）：不再弹「数据同步失败」横幅、不再被误踢到登录页；离线数据入云仍走登录后「预览并合并」

## [0.1.0] - 2026-08-29

### ✨ 新增（M1~M4 已完成；本次为 M5 收尾）

- **周报/月报**：生成快照落库（周报含完成率/环比/分类/最佳提醒/建议；月报含热力图），通知中心可跳转，周一/1 日 08:00 自动生成（cron 可配置），支持手动触发（设置 → 我的数据 → 周报/月报）
- **成就系统**：7 项内置成就（连续 7/30/100/365 天、用药 100 次、锻炼 50 次、喝水 200 次）；提醒完成后自动检查，解锁即通知+推送；成就墙页含进度与社区分享
- **数据导出/注销**：一键导出全量数据（JSON 下载）；注销账号即时清理数据（审计留存），旧登录态立即失效
- **部署支持**：`docs/部署手册.md`（Nginx+PM2/Docker/Migration 策略）+ 数据库备份脚本

### 🎨 UI

- 全页面视觉评审通过（18 页 ≥8）：修复提醒列表快捷块文案孤行；热力图 4 档配色；路由懒加载分包（首屏主包 311KB）
- 新增页面：报告列表/详情、成就墙；设置页"我的数据"区（报告/成就/导出/注销）

### 🛡️ 安全与可靠性

- 全局限流（100 次/分/用户；登录/注册 5 次/分/IP）+ 登录失败 5 次锁定 15 分钟
- 审计日志（注册/登录/删除类操作）；敏感词过滤（发帖/评论）；上传魔数校验 + 失败清理；SQLite WAL 生效
- Web Push 全链路：自定义 Service Worker（push/notificationclick）+ 提醒到期/漏服/低库存自动推送
- 离线同步队列：提醒操作离线入队，网络恢复自动重放（后端幂等保证不重复扣减）

### 🧪 质量

- 后端 Jest 61 用例（新增 28：锁定/审计/敏感词/魔数/到期推送/报表聚合/成就规则）
- 前端 Vitest 20 用例（新增 11：离线队列/zod schema/VAPID key 转换）
- `npm audit --omit=dev`：0 漏洞（官方 registry）

### 📝 注意

- 注销采用**演示口径**：即时物理清理业务数据（未做 30 天缓冲撤销）
- UI 评审已迁移至内置多模态视觉（原 ARK 脚本废弃）；`.env` 无需 ARK_API_KEY
- 页面级截图工具：`node ../tools/scripts/capture-ui.mjs`（在 frontend/ 下运行）
