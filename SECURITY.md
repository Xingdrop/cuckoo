<!-- <!-- @Sdrop 布谷(Cuckoo) v2 SKEY_5biD6LC3KEN1Y2tvbyl8WGluZ2Ryb3B8U0VDVVJJVFkubWR8MjAyNi0wOXxhODVjMzViNDk4 --> -->
# 安全政策（Security Policy）

## 支持版本
| 版本 | 安全支持 |
|---|---|
| main（当前开发版） | ✅ 支持 |
| 历史里程碑（M1~M5 等） | 仅重大漏洞 |

## 报告漏洞
请**私密**报告，不要公开 issue/PR 讨论漏洞细节（先修复后披露）：

| 渠道 | 方式 |
|---|---|
| GitHub 私密安全公告（推荐） | Repo → Security → Report a vulnerability |
| 邮箱 | 通过仓库 ISSUE 或项目主页联系作者获取 |

**请在报告中包含**：影响版本、复现步骤（或最小 POC）、预期/实际行为、影响面（数据/账户/可用性）。

## 承诺响应时间
| 严重性 | 首次响应 | 修复目标 |
|---|---|---|
| 严重（数据泄露/RCE） | 72 小时内 | 7 天内（或提供临时缓解） |
| 高 | 1 周内 | 2 周内 |
| 中/低 | 2 周内 | 按里程碑计划 |

> 本项目非商业维护，响应时间以作者可用时间为准，但严重漏洞优先。

## 严重性分级（参考 CVSS 3.x）
- **严重**：远程代码执行、任意文件读写、认证绕过、批量数据泄露、SQL 注入
- **高**：越权访问（IDOR）、敏感信息明文暴露、存储型 XSS、任意文件上传
- **中**：反射型 XSS、CSRF、目录遍历（受限）、限流绕过
- **低**：信息泄露（低价值）、CSP 缺陷、日志敏感字段

## 已加固清单（2026-08 - 供安全研究者参考）
- 认证：JWT + 密码 bcrypt + 登录 5 次/分/IP 锁定 15 分钟 + 全局 Throttler
- 输入：class-validator DTO 全量校验 + zod 前端对齐 + 敏感词过滤（帖子/评论）
- 上传：扩展名/MIME/魔数三重校验 + sharp 重编码去 EXIF + 失败清理 + 类型化大小上限
- 数据：SQLite WAL、参数化查询（TypeORM）、审计日志（关键操作）
- 部署建议：helmet + 反向代理（Nginx/PM2，见项目发行材料）；生产密钥仅在 `.env`（gitignored）

## 披露流程
1. 开发者确认并修复（含补丁与致谢，如报告者同意列名）
2. 修复后 90 天内发布详细公告（版本、影响、缓解、CVE 若已分配）
3. 公共讨论渠道：仅公告；详细利用细节不公开
