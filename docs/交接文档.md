# 布谷（Cuckoo）项目交接文档

> 版本：v1.0 ｜ 日期：2026-08-22 ｜ 交接对象：下一个接力开发 AI
> 关联文档：《需求分析文档.md》（FR 编号来源）、《技术方案设计.md》、《项目计划与单元测试验收文档.md》（任务勾选状态）
> **阅读本文件后，请先读《项目计划与单元测试验收文档.md》§0 进度总表，继续勾选推进 M5。**

---

## 0. 一句话交接

项目是 **Web PWA（React 19 + TS + Vite + Tailwind v4 + Zustand）前端 + NestJS 11 + TypeORM + better-sqlite3 后端** 的智能提醒与健康管理社交应用。**M0~M4 已完成并全部提交**，当前停在 M4 收尾；下一步是 **M5 里程碑**（周报月报生成、热力图完善、成就系统、数据导出/注销、部署手册、UI 全面评审与发布检查）。

---

## 1. 项目概览

### 1.1 产品定位
以"定时提醒"为核心，融合用药管理、短时锻炼、生活习惯养成与社交激励的健康管理应用。Slogan：*准时提醒，温柔守护——布谷，你的健康生活管家。* 完整需求见 `docs/需求分析文档.md` 与 `docx/布谷企划-app制作-extracted.md`。

### 1.2 技术栈（已决策，勿更换）

| 层 | 技术 |
|---|---|
| 前端 | Vite 8 + React 19 + TypeScript + Tailwind CSS v4（CSS 变量 design tokens）+ Zustand（persist）+ React Router 7 + Recharts + date-fns + lunar-javascript + zod + lucide-react |
| PWA | vite-plugin-pwa（devOptions.enabled 开发期可用 SW），Service Worker + Web Push（web-push，VAPID 密钥） |
| 后端 | NestJS 11 + TypeORM 1.1.0 + better-sqlite3 12 + @nestjs/schedule（cron/Interval）+ JWT（passport-jwt）+ class-validator + Swagger + helmet + multer + sharp |
| 测试 | 后端 Jest（含内存 SQLite 集成测试）；前端 Vitest + Testing Library；E2E 用 Playwright（Node 脚本 `frontend/tests/`）+ Python Playwright（webapp-testing skill） |
| UI 评审 | 火山方舟豆包视觉模型 `tools/ui_review.py` + `.zcode/skills/ui-review`（需 `ARK_API_KEY`，见 §9） |

### 1.3 目录结构

```
E:\ASEE\项目\Prg-2 布谷\
├── docs/                      # 需求分析/技术方案/项目计划/本交接文档/源文档
├── docx/                      # 原始需求 docx + 提取的 md
├── backend/                   # NestJS API
│   ├── src/
│   │   ├── main.ts            # 启动：helmet/CORS/ValidationPipe/全局异常过滤/路由前缀 api/v1/Swagger
│   │   ├── app.module.ts      # 模块装配 + 全局 JwtAuthGuard + TypeORM entities 注册（新实体必须加！）
│   │   ├── config/configuration.ts   # 环境变量集中读取（新增参数必须同步 .env.example 与技术方案 §9）
│   │   ├── common/            # 调度算法纯函数、utcDateTime transformer、守卫/装饰器/异常过滤器
│   │   ├── modules/           # auth/users/reminders/medicines/stats/exercises/files/social/notifications/contacts/health
│   │   └── seed/              # 种子数据：官方计划/微运动库/敏感词（SEED_DEMO_DATA 开关）
│   ├── data/cuckoo.sqlite     # SQLite 数据库（运行时生成）
│   ├── uploads/YYYYMM/        # 上传图片（webp 压缩 + 缩略图）
│   └── .env / .env.example
├── frontend/
│   ├── src/
│   │   ├── app/App.tsx        # 路由表（见 §5.2）
│   │   ├── components/        # BottomNav/ConfirmModal/RequireAuth
│   │   ├── features/reminders/# ReminderScheduler/useReminderScheduler/ReminderOverlay（提醒触发前端通道）
│   │   ├── pages/             # 16 个页面组件
│   │   ├── services/          # http.ts（唯一 axios 出口）+ api.*.ts 按领域封装
│   │   ├── stores/authStore.ts# zustand + persist
│   │   ├── types/index.ts     # 与后端 DTO 对齐的共享类型
│   │   └── utils/             # reminder-schedule（与后端同源）/calendar（农历节日）/push
│   └── tests/unit/            # Vitest 单测（reminder-schedule.spec.ts 等，vitest include 匹配该目录）
├── tools/                     # ui_review.py（ARK 视觉评审）
├── .zcode/skills/             # 项目级 skills（见 §10）
└── Plugin/                    # 第三方插件包原始文件（已安装到 .zcode/skills/）
```

---

## 2. 已完成内容（M0 ~ M4）

> git 历史：`git log --oneline`，共 20+ 提交，遵循 Conventional Commits（feat/fix/test/docs/chore），全部在 main 分支。

### M0 工程地基（提交 `a584268` → `186d6d9`）
- 前后端脚手架、设计系统 tokens（Tailwind v4 CSS 变量）、.env.example、CI npm scripts、git 工作流约定
- 安装了 Vercel 系 skills（react-best-practices / composition-patterns / frontend-design / web-design-guidelines / webapp-testing / ui-review 等）替换原 Plugin 中不适用的 skill

### M1 认证 + 提醒核心（`f5524dc` → `d167ff1`）
- 后端：注册/登录/JWT（bcrypt 哈希）、提醒 CRUD + 软删除、调度算法纯函数、执行记录（完成/延迟/跳过）、API 根路由元信息、全局异常过滤器（错误码归一化 `code/message`）
- 前端：登录注册页、提醒列表/编辑页、**全屏提醒弹窗（ReminderOverlay）**、Web Push 订阅（`utils/push.ts`）、本地调度器（setTimeout 按 nextTriggerAt 触发）、通知偏好设置
- 双通道提醒可靠性：**本地调度器 + Web Push 兜底**；页面打开时本地 setTimeout 触发，页面关闭靠 Push

### M2 用药管理（`53452c6`）
- 药品 CRUD（名称/剂量/服用方式/库存/阈值/有效期/服用说明/照片/每次扣减量）、用药提醒关联（弹窗带药品照片与剂量）、**库存事务扣减**（确认服药自动扣减 + 手动调整 + PRN 手动记录）、**库存预警通知**（扣减后 ≤ 阈值 → 用户通知）、服药历史分页

### M3 统计 + 日常 + 打卡（`2bacc2b` → `2a709e8`）
- 今日看板（日期切换 + 左右滑动 + 农历节日 + 已完成/未完成/已错过分组 + k/n 次数徽标 + 喝水进度卡 + 统计入口）、统计聚合（完成率 = done/planned，skip 不计）、连续天数、热力图、趋势图、分类统计
- 喝水提醒（waterAmount/waterGoalMl 设置，达标变色）、番茄钟、微运动库（种子）、拍照打卡挑战（相机/相册 → 上传 → challenge_completed）
- 多项 UI 反馈修复：导航重构（今日/提醒/社交三 tab）、快捷功能长块（+ 下方：药物/喝水/锻炼/番茄）、自定义分类图标选择器（16 预设 emoji 网格）、自定义延迟分钟、删除确认弹窗（ConfirmModal）

### M4 社交 + 亲友通知（`55afbd9` → `40a8c1d`）
- 社交后端：帖子（图文 + planSnapshot）、点赞/收藏幂等、评论分页、**一键加入计划**（事务：快照解析 → 批量建提醒 → PlanJoinRecord → joinedCount 原子 +1，幂等防重复）、退出计划、**官方计划模板**（种子数据，一键加入）、**兴趣小组**（创建/加入/组内帖子/排行榜 service 已实现）、敏感词过滤（种子数据，敏感词替换 `**`）
- 通知中心：列表 + 未读计数 + 标记已读（单条/全部）
- **漏服扫描（missed-scanner.ts）**：每分钟 @Interval 扫描，超过 30 分钟（阈值）未响应的提醒 → 置 missed + 通知本人 + 通知开启 `receiveMissed` 的亲友（dedupKey 幂等）
- 前端：SocialPage（帖子流/官方计划/兴趣小组三 tab + 发帖 composer）、PostDetailPage、NotificationsPage（🔔 铃铛入口）、通知红点

---

## 3. 未完成内容（下一步工作）

### 3.1 M5 里程碑（项目计划 §1.2，6 个 WBS 任务全部 ☐）
按顺序推进（后端先行）：

1. **周报/月报生成**（约 2.5 人日）
   - 后端：`modules/reports/` 新模块。cron 已预留：`WEEKLY_REPORT_CRON`（默认每周一 08:00）、`MONTHLY_REPORT_CRON`（默认每月 1 日 08:00）。生成报告数据 → 写 Notification（类型新增 `weekly_report` / `monthly_report`）→ 用户点击跳报告页
   - 周报内容：本周完成率、分类完成统计、与上周对比、最佳分类和提醒、下周建议文案
   - 月报内容：月度热力图、习惯养成分析（坚持天数分布）、成就总结
   - 前端：报告页面（复用 StatsPage 的 recharts 组件）
   - 验收：AC-601、AC-602

2. **热力图完善**（约 1.5 人日）
   - `GET /api/v1/stats/heatmap?month=YYYY-MM` 已有实现；前端 StatsPage 已有 grid 渲染
   - 需要：颜色映射完成率完善（0/1-49/50-99/100% 四档）、月份切换导航、周报月报中引用

3. **成就系统**（约 2 人日）
   - 后端：`modules/achievements/`，Achievement entity（类型：streak_7/30/100/365、medication_100、exercise_50、water_200）+ 检查服务（在 ack / stats 链路后触发，幂等：同一成就只入表一次）
   - 前端：成就墙页面（达成后可分享到社区，生成含快照的帖子）
   - 验收：AC-603

4. **数据导出/注销**（约 1.5 人日）
   - `GET /api/v1/users/me/export` → 下载 JSON 含全部用户数据（提醒/日志/药品/帖子/设置）
   - `DELETE /api/v1/users/me` → 注销（软删除 + 30 天缓冲后物理清除，审计留存；或直接物理删除演示即可，注意文档标注）
   - 验收：AC-105、AC-106、IT-06

5. **部署脚本 + 部署手册**（约 1 人日）
   - 产出 `docs/部署手册.md`（Nginx + PM2 或 Docker 方案），生产模式注意 `NODE_ENV=production` 时 `synchronize: false`（需 migration 策略）
   - 数据备份脚本

6. **UI 全页面评审 + 全量回归 + 发布**（约 2 人日）
   - 每个页面截图（390×844）→ `python tools/ui_review.py <截图> "<页面说明>"` 评分 ≥ 8
   - 里程碑结束 `--dir tools/screenshots` 批量复评
   - 按项目计划 §9 Release Checklist 逐项勾选

### 3.2 计划文档中已列但尚未实现的测试项（与 M5 同步补）
- 后端单测：UT-STAT-*（8 个）、UT-MISS-*（7 个）、UT-JOIN-*（6 个）、UT-COMMON-*（8 个）、UT-AUTH 部分（密码强度/锁定/过期/越权）——当前只有 33 个单测（auth/reminder-schedule/medicines），计划里标记 ☐ 的都要补
- 集成测试 IT-01~08、E2E-01~10 的正式脚本化。**现状**：`frontend/package.json` 已声明 `"test:e2e": "playwright test"` 且装有 `@playwright/test`，但**仓库内没有 playwright.config 也没有 e2e 用例目录**（历次里程碑 E2E 均为会话内临时脚本，未沉淀）；M5 需补 `frontend/e2e/` + playwright.config.ts，把 E2E-01~10 固化为可重复运行的套件
- 前端 Vitest 计划用例 UT-FE-01~10

### 3.3 已知可选的 P1 增强（已具备后端基础，未做前端）
- **亲友联系人管理前端页面**：`EmergencyContact` entity 已存在（`backend/src/modules/contacts/`），漏服扫描已使用它通知亲友，但**没有 controller/service 的 CRUD API，也没有前端页面**。需要：contacts 模块 CRUD + 设置页入口 + 表单
- **Web Push 到期提醒链路未打通（重要）**：前端订阅链路完整（`utils/push.ts` + `POST /devices`），但 SW 由 `vite-plugin-pwa` 按 workbox 自动生成（`vite.config.ts` 仅配置了 precache + `/api` 路由 NetworkFirst 运行时缓存），**没有自定义 push / notificationclick 事件处理器**——意味着订阅成功后，服务端 `web-push` 推送到达浏览器时默认 SW 不会弹出通知。要打通"页面关闭仍能收到提醒"：需自定义 SW（vite-plugin-pwa 的 `injectRegister` + 自定义 `sw.js` 或 `workbox` 配置扩展），处理 `push`（event.data.json() → showNotification）与 `notificationclick`（focus/打开 `/today`），再在提醒到期/漏服/库存预警处调用 `pushService.sendToUser`（目前这些场景只写 Notification 表，未推系统通知）
- 帖子转发（share interaction）、收藏列表页、加入用户列表
- 排行榜前端（后端 `groupLeaderboard` 已实现，SocialPage 未接）
- 敏感词过滤已实现（种子 + 过滤逻辑），未接前端提示

---

## 4. 如何运行

### 4.1 首次准备
```bash
# 后端
cd backend
cp .env.example .env      # 按需填 VAPID 密钥（npx web-push generate-vapid-keys）
npm install

# 前端
cd ../frontend
npm install
```

### 4.2 启动开发
```bash
# 终端 1：后端（默认 http://localhost:3000）
cd backend && npm run start:dev

# 终端 2：前端（默认 http://localhost:5173，/api 代理到 3000）
cd frontend && npm run dev
```
访问 `http://localhost:5173`。**注意**：如果 5173 被占用 Vite 会换端口（前端代理与 CORS 白名单都按 5173 配置）；如果 API 行为异常（如 500 / 404），先检查是否有旧 node 进程占用 3000 端口——**`taskkill //F //IM node.exe` 后干净重启**（历史踩坑：旧进程继续服务旧代码导致"新功能不生效"假象）。

### 4.3 测试
```bash
cd backend && npm test          # Jest 单测（当前 33 通过）
cd frontend && npm test         # Vitest（当前 9 通过，tests/unit/ 目录）
cd frontend && npm run test:e2e # Playwright——注意：当前仓库尚无 playwright.config 与用例（见 §3.2），此命令 M5 补完 E2E 套件后才可用
```

### 4.4 Swagger / 调试
- 启动后端后访问 `http://localhost:3000/api`（非 production 环境）——全部接口的 OpenAPI 文档，带 Bearer 认证说明
- `http://localhost:3000/api/v1/health` 健康检查；`http://localhost:3000/api/v1` API 元信息

### 4.5 UI 视觉评审（每次改 UI 后）
```bash
python tools/ui_review.py <截图路径> "<页面说明>"
# 批量：python tools/ui_review.py --dir tools/screenshots
```
需要 `ARK_API_KEY` 环境变量（见 §9）。截图方式：浏览器 devtools 390×844 视口截图，或 Playwright 截图。

---

## 5. 前端模块与页面

### 5.1 前端服务层（`frontend/src/services/`）
所有 HTTP 请求必须走 `services/http.ts` 的全局 axios 实例（自动注入 token、401 跳登录、错误归一化 `errorMessage(e)`）。按领域封装在 `services/api/`：

| 文件 | 导出 | 对应后端 |
|---|---|---|
| `api.auth.ts` | `authApi.register/login` | POST /auth/* |
| `api.reminders.ts` | `remindersApi`（list/today/calendar/create/update/remove/setActive/ack/delay/logs）+ `CreateReminderInput` | /reminders/* |
| `api.medicines.ts` | `medicinesApi` + `MedicineInput` | /medicines/* |
| `api.stats.ts` | `statsApi`（dashboard/heatmap/trend/water）+ `DashboardStats/DayStat` | /stats/* |
| `api.social.ts` | `socialApi`（posts/templates/groups…）+ `Post/PlanTemplate/Group`；`notificationsApi` | /posts/*、/plan-templates/*、/groups/*、/notifications |

### 5.2 路由表（`app/App.tsx`，全部 RequireAuth 包裹）

| 路径 | 页面 | 说明 |
|---|---|---|
| `/login` | LoginPage | 登录/注册切换 |
| `/today` | DashboardPage | 今日看板（日期滑动/农历/分组/喝水卡/统计入口） |
| `/reminders` | ReminderListPage | 提醒列表（+ 下方 4 快捷长块：药物/喝水/锻炼/番茄） |
| `/reminders/new`、`/reminders/:id/edit` | ReminderEditPage | 创建/编辑（分类图标/多时间点/喝水设置/拍照挑战） |
| `/medicines`、`/medicines/new`、`/medicines/:id/edit`、`/medicines/:id/logs` | MedicinesPage 系列 | 药品管理（含返回按钮） |
| `/stats` | StatsPage | 统计（热力图 grid + recharts 趋势 + 分类条形） |
| `/pomodoro` | PomodoroPage | 番茄钟 |
| `/water-settings` | WaterSettingsPage | 喝水设置（开关/每次水量/每日目标/新建提醒） |
| `/exercises` | ExercisesPage | 微运动库 |
| `/social` | SocialPage | 社区（帖子流/官方计划/小组三 tab + 发帖） |
| `/posts/:id` | PostDetailPage | 帖子详情 |
| `/notifications` | NotificationsPage | 通知中心 |
| `/settings` | SettingsPage | 设置（通知偏好等） |
| `*` | → /today | 兜底重定向 |

### 5.3 提醒触发机制（前端侧，重点理解）
`features/reminders/ReminderScheduler.tsx`（挂载在 App 根）+ `useReminderScheduler.ts`：
1. 登录后轮询 `GET /api/v1/reminders/today`（15s）+ 监听事件总线 `cuckoo:reminders-changed`（增删改提醒后触发刷新）
2. 从 `nextTriggerAt` 计算最近触发点，setTimeout 到时弹出 `ReminderOverlay` 全屏弹窗
3. 弹窗操作：完成（可带拍照挑战 flow）/ 延迟（预设 + 自定义分钟）/ 跳过 → 调 `POST /api/v1/reminders/:id/ack` 或 `:id/delay`
4. 页面关闭时：依赖 Web Push / SW（PWA 安装后 `push.ts` 订阅）。**注意：到期 Push 触发链路尚未完全打通，见 §3.3**

### 5.4 前端工具库
- `utils/reminder-schedule.ts`：**与后端同源**的调度算法（见 §7.1），任何时间计算都用它
- `utils/calendar.ts`：`dateKey/todayKey/shiftKey/dayLabel/dateHead/lunarInfo/festivalIcon/navKeys`——看板日期切换 + 农历（lunar-javascript）+ 节日图标
- `utils/push.ts`：`isPushSupported/subscribePush/unsubscribePush/checkPushSubscribed/pushFailMessage`——VAPID 订阅全流程

---

## 6. 后端 API 接口全表（前缀 `/api/v1`，除标注外全部需 `Authorization: Bearer <token>`）

> 错误统一格式：`{ "code": "VALIDATION_FAILED" | "UNAUTHORIZED" | "FORBIDDEN" | "NOT_FOUND" | "CONFLICT" | "INTERNAL_ERROR" | ..., "message": "..." }`（AllExceptionsFilter 归一化）。

### 6.1 认证（公开）
| 方法 | 路径 | 请求体 | 说明 |
|---|---|---|---|
| POST | `/auth/register` | `{username, password}` | 注册，返回 `{token, user}`；用户名重复 → CONFLICT |
| POST | `/auth/login` | `{username, password}` | 登录（HTTP 200），返回 `{token, user}`；错误密码 → 401 |

### 6.2 用户
| 方法 | 路径 | 请求体 | 说明 |
|---|---|---|---|
| GET | `/users/me` | — | 当前用户资料（含 settings） |
| PATCH | `/users/me` | `{avatarUrl?, healthGoals?, timezone?}` | 更新资料 |
| GET | `/users/me/settings` | — | 通知偏好 |
| PUT | `/users/me/settings` | `{notificationEnabled?, soundEnabled?, vibrationEnabled?, theme?, missedThresholdMinutes?, showSkipButton?, maxDelayCount?, waterGoalMl?}` | **部分更新**（只更新传入字段，返回最新全量 settings；waterGoalMl 范围 100~10000） |

### 6.3 提醒（核心）
| 方法 | 路径 | 参数/请求体 | 说明 |
|---|---|---|---|
| GET | `/reminders` | query: `category?`, `isActive?` | 全部提醒 |
| GET | `/reminders/today` | — | 今日概览：将触发 + 已执行（含 k/n 次数） |
| GET | `/reminders/calendar?date=YYYY-MM-DD` | query: `date` | **指定日期规划**（看板日期切换数据源，含 nextTriggerAt） |
| POST | `/reminders` | CreateReminderDto | 创建（见 §7.2 字段） |
| GET | `/reminders/:id` | — | 详情 |
| PUT | `/reminders/:id` | UpdateReminderDto | 更新 |
| DELETE | `/reminders/:id` | — | 软删除 |
| PATCH | `/reminders/:id/active` | body: `{isActive: boolean}` | 启停 |
| POST | `/reminders/:id/ack` | `{status: 'completed'\|'skipped'\|'challenge_completed', minutes?（延迟后完成时）, photoUrl?}` | **执行上报，幂等**（UNIQUE reminderId+scheduledTime）；用药提醒完成自动扣库存 |
| POST | `/reminders/:id/delay` | `{minutes: 1~1440}` | 延迟后重新调度 |
| GET | `/reminders/:id/logs?page=&pageSize=` | — | 执行记录分页 |

### 6.4 药品
| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/medicines` | 药品列表 |
| POST | `/medicines` | 添加（name/dosage/administration/stock/threshold/expiryDate/instructions/photoUrl/deductionPerUse） |
| GET/PUT/DELETE | `/medicines/:id` | 详情/编辑（软删除）/删除 |
| PATCH | `/medicines/:id/stock` | body `{delta: -10000~10000}`，手动调整库存（补充为正） |
| POST | `/medicines/:id/deduct` | **PRN 手动记录**：扣库存 + 写 ReminderLog（reminderId 可空）+ 触发预警检查 |
| GET | `/medicines/:id/logs` | 服药历史分页 |

### 6.5 统计
| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/stats/dashboard` | 今日看板统计：完成率/连续天数/分类统计/喝水进度（完成率 = done/planned，skip 不计） |
| GET | `/stats/heatmap?month=YYYY-MM` | 月热力图（默认当月） |
| GET | `/stats/trend?days=7` | 近 N 天趋势 |
| POST | `/stats/water` | `{amountMl: 1~5000}` 手动记录喝水 |

### 6.6 微运动
| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/exercises` | 微运动库（种子数据，isActive 过滤） |

### 6.7 社交
| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/posts?page=&pageSize=` | 帖子流（作者/点赞/收藏/评论/加入人数） |
| POST | `/posts` | `{content, mediaUrls?, type?, planSnapshot?}` 发布（含计划快照可一键加入） |
| GET/DELETE | `/posts/:id` | 详情（含我是否点赞/收藏/已加入）/删除 |
| POST | `/posts/:id/like` | 点赞/取消（幂等 toggle） |
| POST | `/posts/:id/favorite` | 收藏/取消（幂等 toggle） |
| POST | `/posts/:id/comment` | `{content}` 评论 |
| GET | `/posts/:id/comments?page=&pageSize=` | 评论列表 |
| POST | `/posts/:id/join` | **一键加入计划**（事务+幂等，joinedCount+1） |
| DELETE | `/posts/:id/join` | 退出计划（软删提醒 + count-1） |
| GET | `/plan-templates` | 官方计划模板列表 |
| POST | `/plan-templates/:id/join` | 加入官方计划（批量建提醒） |
| GET | `/groups` | 小组列表 |
| POST | `/groups` | `{name, description}` 创建小组 |
| POST | `/groups/:id/join` | 加入小组 |
| GET | `/groups/:id/posts` | 组内帖子 |

### 6.8 通知 / 推送
| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/notifications?page=&pageSize=` | 通知列表，返回 `{items, total, unread}` |
| PATCH | `/notifications/read` | `{id?}` 标记已读（缺省 = 全部已读） |
| GET | `/push/vapid-key` | **公开**：VAPID 公钥（前端订阅用） |
| GET | `/devices` | 我的推送设备 |
| POST | `/devices` | `{endpoint, keysAuth, keysP256dh, userAgent?}` 注册订阅（幂等） |
| DELETE | `/devices/:id` | 退订 |

### 6.9 文件 / 系统
| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/files/upload` | multipart 字段 `file`（jpg/png/webp ≤10MB）→ sharp 压缩 webp + 缩略图 → 返回 `{url, thumbUrl}`（如 `/uploads/202608/xxx.webp`） |
| GET | `/health` | 公开：健康检查（查 DB） |
| GET | `/` | 公开：API 元信息 |

---

## 7. 核心模块与关键逻辑（务必先理解再改）

### 7.1 调度算法（前后端同源）
- 后端 `backend/src/common/reminder-schedule.ts`，前端镜像 `frontend/src/utils/reminder-schedule.ts`——**改动必须双端同步并跑两边单测**
- 导出纯函数：
  - `computeNextTrigger(rule, from, startDate, endDate, timezone, times?)` → 下次触发 Date 或 null
  - `computeFollowingTrigger(rule, from, ...)` → 再下次（延迟/循环用）
  - `toLocal(d, tz)` / `localToUtc(...)`：时区转换（Intl formatToParts + 迭代校正，DST 安全）
- 语义要点（踩坑总结）：
  - repeat type：`once` / `daily` / `weekly`（daysOfWeek 多选）/ `monthly`（dayOfMonth，月末顺延）/ `interval`（intervalUnit: day/hour/week）
  - **`times: string[]`（HH:mm）多时间点**：daily/weekly 时每天从各 times 生成候选
  - **interval hour = 每天从 startDate 时刻开始循环到午夜**（不是简单的 +N 小时跨天）；interval day/week = 绝对步进
- 已由 23+ 单测覆盖（后端 `reminder-schedule.spec.ts`）

### 7.2 提醒实体字段（CreateReminderDto 要点）
```
category: 'medication'|'exercise'|'water'|'rest'|'work'|'custom'
categoryLabel/categoryIcon（custom 分类的文本与 emoji 图标）
title, repeatRule{type, daysOfWeek?, dayOfMonth?, intervalValue?, intervalUnit?}
startDate, endDate?, times?: string[]（多时间点 HH:mm）
content{text?, imageUrls?, videoUrl?, jumpTo?, waterAmountMl?, waterGoalMl?}
method{useFullScreen, sound, vibrationEnabled, gradualSound}
delaySettings{presetOptions?, customEnabled, maxDelayCount}
challenge{enabled, type:'photo', allowGallery}
showSkipButton, medicineId?（用药关联）
```
> **注意**：`content.waterAmountMl` / `waterGoalMl` 必须在 ReminderContentDto 声明（ValidationPipe forbidNonWhitelisted，未声明 → 400）。

### 7.3 统计口径（勿改，除非有明确需求）
- 完成率 = done / planned（**skip 不计入分母**，missed 计入）
- 连续天数：从今天（或昨天，今天未结）向前回溯全完成天数
- 分类统计：ReminderLog 存 category/amount **快照**（改分类不影响历史）
- 每日数据 = `remindersService.dayPlan(userId, dateStr)`（日历/统计复用）
- 喝水目标：`UserSetting.waterGoalMl`（默认 2000），每次确认喝水提醒扣 `content.waterAmountMl ?? 200`

### 7.4 库存事务与预警
`medicines.service.ts`：`deductStock` 在 `dataSource.transaction` 内完成 扣减 → 写 ReminderLog（含 stockDeducted 快照）→ 检查 ≤ threshold → 写 Notification（`type: 'low_stock'`）。扣减量 > 库存 → `STOCK_EXCEEDED` 回滚。幂等：UNIQUE(reminderId, scheduledTime) 保证重复 ack 只扣一次。

### 7.5 一键加入计划（事务要点）
`social.service.ts#joinPlan`：transaction 内 校验 planSnapshot → 解析 `reminderConfig[]`（category/title/repeatRule/startTime/content）→ 逐条创建提醒 → 写 PlanJoinRecord（UNIQUE(postId,userId) 幂等）→ `UPDATE post SET joinedCount = joinedCount + 1`（原子）。失败回滚零副作用。

### 7.6 漏服扫描（missed-scanner.ts）
`@Interval('missed-scan', 60_000)`（env 可调）：`nextTriggerAt < now` 且该时刻无任何响应记录 且 超时 > 30 分钟（用户级 `missedThresholdMinutes` 可调）→ 写 missed 日志 + 本人通知 + 亲友通知（`receiveMissed=true` 且有 `appUserId`；短信通道 P2 未接）。所有通知去重走 `NotificationLog.dedupKey`。

### 7.7 Web Push（push.service.ts + frontend/utils/push.ts）
- VAPID 密钥在 `.env`（生成：`npx web-push generate-vapid-keys`），未配置时 `sendToUser` 自动降级 no-op
- 前端流程：`isPushSupported()` → Notification.requestPermission → `navigator.serviceWorker.ready` → pushManager.subscribe（applicationServerKey = `GET /push/vapid-key`）→ `POST /devices` 注册
- `sendToUser(userId, {title, body, url})` 向该用户所有设备推送，404/410 失效订阅自动清理
- **现状**：订阅链路完整；提醒到期/漏服/库存预警尚未接入 `sendToUser`（见 §3.3）

### 7.8 文件上传（files.controller.ts）
multer diskStorage → `uploads/YYYYMM/{uuid}.{ext}` → sharp `.rotate().resize(1600).webp(82)` 压缩 + `_thumb` 400px 缩略图 → 删原图 → 返回相对 URL。静态服务：main.ts `app.useStaticAssets(..., {prefix: '/uploads/'})`。

### 7.9 种子数据（seed/seed.service.ts）
启动时写入（`SEED_DEMO_DATA=true` 默认开）：官方计划模板（`tpl-water-schedule` 等，reminderConfig JSON 结构见 §7.5）、微运动库（Exercise，50+ 条）、敏感词（SensitiveWord）。幂等：按业务键存在跳过。

### 7.10 通知类型（notification.entity.ts NotificationType）
`LOW_STOCK`（库存预警）/ `MISSED`（漏服）/ `REMINDER`（待接入）/ `WEEKLY_REPORT` / `MONTHLY_REPORT`（M5 新增）/ `SYSTEM`。

---

## 8. 数据库实体（entities 注册在 app.module.ts，**新增实体必须同步注册 + 文档**）

| 实体 | 文件 | 要点 |
|---|---|---|
| User / UserSetting | `modules/users/` | settings 一对一；waterGoalMl 在 UserSetting |
| Reminder / ReminderLog | `modules/reminders/` | Log：nullable reminderId（PRN 用）、category/amount 快照、UNIQUE(reminderId, scheduledTime) |
| Medicine | `modules/medicines/` | 库存/阈值/扣减量 |
| EmergencyContact | `modules/contacts/` | **无 CRUD API**（§3.3） |
| Post / Interaction / PlanJoinRecord / PlanTemplate / SensitiveWord | `modules/social/` | Interaction UNIQUE(postId,userId,type)；PlanJoinRecord UNIQUE(postId,userId) |
| Group / GroupMember / GroupPost | `modules/social/group.entity.ts` | 小组 |
| Notification / NotificationLog / Device | `modules/notifications/` | Log 的 dedupKey 去重；Device endpoint 唯一 |
| Exercise | `modules/exercises/` | 微运动库 |

### 8.1 TypeORM 关键陷阱（血泪史，勿重蹈）
1. **裸列必须显式 `type: 'varchar'`**：TypeORM 1.x 对 `string | null` 推断失败 → `DataTypeNotSupportedError: Data type Object`
2. **datetime 读取不带时区**：SQLite 返回 `"2026-08-18 15:59:00.000"`，必须用 `utcDateTime` transformer 读取时补 `Z`（`common/datetime.transformer.ts`）
3. **CreateDateColumn + transformer**：`to()` 对 undefined 必须返回 undefined（返回 null → NOT NULL constraint failed）
4. **禁用 save(loadedEntity) 更新**：TypeORM 1.1.0 对带 transformer 的列用 save() 会写入数据库旧值（DEBUG SQL 可见 `SET xxx = 0` 反向写入）。**所有更新一律 `repo.update({id, userId}, patch)`**（push.service 注释也标了）
5. **不要自定义 boolColumn/intColumn transformers**：1.1.0 的 `preparePersistentValue` 会二次转换导致 bool 写入 0
6. 新实体注册遗漏 → 运行时 500（Group 曾踩过）；**改完代码若行为"没变化"，先 `taskkill //F //IM node.exe` 重启**（增量编译的 dist 会过期，`rm -rf dist` 兜底）

---

## 9. 环境变量与可转移参数（.env.example 为准）

| 变量 | 默认 | 用途 |
|---|---|---|
| NODE_ENV | development | production 时关 Swagger / synchronize |
| PORT | 3000 | 后端端口 |
| CORS_ORIGINS | http://localhost:5173 | CORS 白名单，逗号分隔 |
| DB_PATH / DB_WAL | data/cuckoo.sqlite / true | SQLite 文件与 WAL |
| JWT_SECRET / JWT_EXPIRES_IN | 占位 / 7d | **生产必须替换** |
| UPLOAD_DIR / MAX_FILE_SIZE | uploads / 10485760 | 上传目录与限制 |
| VAPID_PUBLIC_KEY / VAPID_PRIVATE_KEY / VAPID_SUBJECT | 空 | Web Push（生成：npx web-push generate-vapid-keys） |
| **ARK_API_KEY / ARK_MODEL** | ark-ec4ac84a-...-ed77b / doubao-seed-2-0-pro-260215 | **UI 评审工具用**（tools/ui_review.py + .zcode/skills/ui-review），**通过环境变量注入，绝不硬编码进代码** |
| MISSED_SCAN_INTERVAL_MS | 60000 | 漏服扫描间隔 |
| WEEKLY_REPORT_CRON / MONTHLY_REPORT_CRON | 0 8 * * 1 / 0 8 1 * * | M5 周报月报 cron（配置项已预留） |
| SMS_PROVIDER | none | 短信通道预留（aliyun/twilio，P2） |
| SEED_DEMO_DATA | true | 种子数据开关 |

**规则**：新增可转移参数 → 同步改 `configuration.ts` + `.env.example` + 《技术方案设计.md》§9，三者缺一不可。

---

## 10. Skills / 工具清单（.zcode/skills/ 与插件缓存）

| Skill | 用途 | 何时用 |
|---|---|---|
| `ui-review`（项目级） | ARK 豆包视觉评审 UI 截图，评分 ≥ 8 门禁 | 每个页面完成后 |
| `webapp-testing`（项目级） | Python Playwright 黑盒测试本地 Web 应用 | 功能验证、手势类 E2E |
| `frontend-design` / `web-design-guidelines` / `react-best-practices` / `composition-patterns` / `react-view-transitions` / `writing-guidelines` | 设计/代码规范 | 写前端 UI 与组件时 |
| `requesting-code-review` | 里程碑结束代码评审 | 每个里程碑收尾 |
| `android-emulator:android-dev` | Android 模拟器（插件） | 若需真机/模拟器验证 PWA |
| `docx` / `pdf` / `pptx`（document-skills 插件） | 文档处理 | 读 docx 需求/产报告 |
| `skill-creator` | 创建/编辑 skill | 需要新 skill 时 |

`Plugin/` 目录是第三方插件包原始下载物（ui-theme-designer、agent-skills 等），已评估并安装可用部分到 `.zcode/skills/`，不需要重复安装。

---

## 11. 开发约定与工作流（必须遵守）

1. **git**：全程使用；`main` 分支；Conventional Commits；每个里程碑独立 commit（如 `feat(M4): ...`）
2. **文档同步**：完成任何任务 → 更新《项目计划与单元测试验收文档.md》勾选状态（☐→☑）与 §0 进度总表；新增 API → 同步本交接文档与 Swagger 自动生成
3. **测试先行**：纯逻辑（调度/统计/事务）先写单测再实现
4. **TypeORM 陷阱**：见 §8.1，改实体/更新逻辑时对照检查
5. **UI 每次改动** → 截图 → `ui-review` ≥ 8 再提交
6. **双端调度算法同步**：改 `reminder-schedule.ts` 必须同时改 `frontend/src/utils/reminder-schedule.ts` 并跑两边测试
7. 错误码规范：业务异常用 `{code, message}`（AllExceptionsFilter），不要在 controller 层 try/catch 吞错误
8. 所有页面/组件禁止直接 `import axios`，必须走 `services/http.ts`
9. 敏感信息（ARK_API_KEY 等）只用环境变量，代码/文档中出现即视为违规

---

## 12. 建议的下一步行动清单（M5 第一周节奏）

1. `git pull` 确认在最新 `40a8c1d`；跑 `npm test`（双端）确认基线全绿
2. 后端：新建 `modules/reports/`（周报月报生成 + cron + Notification 类型扩展 + 报告查询 API）
3. 后端：`modules/achievements/`（实体 + check 服务 + ack 链路挂接）
4. 后端：`users/me/export` + `DELETE users/me`（注销）
5. 前端：报告页 + 成就墙 + 统计页完善（热力图月份切换）
6. 前端：设置页接入导出/注销入口；亲友联系人 CRUD 页（§3.3 可选项）
7. 补测试：UT-STAT/UT-MISS/UT-JOIN/UT-COMMON + IT-01~08
8. 部署手册 + Release Checklist 逐项执行 + UI 全页面批量评审
9. 每完成一步：更新计划文档勾选 + commit + 告知用户

---

*本文档随项目推进更新。交接时如有环境问题（端口占用/依赖缺失），先按 §4.2 的 taskkill + 重装流程排查。*
