# 开始.md — 给 AI 开发者的项目启动指引

> 本文件是新项目开工时**第一个交给 AI 读的文档**。模仿它的结构沉淀所有项目共性。
> 读完它，AI 应能回答：这是什么、怎么跑、怎么写代码不踩坑、如何交付、如何安全发布。

---

## 0. 项目快速画像（一次性填写）
- 项目名 / 类型：如「布谷 Cuckoo」— 移动端优先（PWA）的健康提醒 SaaS
- 技术栈：前端 React 19 + TypeScript + Vite 8 + Tailwind v4 + Zustand + React Router 7；后端 NestJS 11 + TypeORM 1.1.0 + better-sqlite3 + JWT + Swagger
- 目录规划：`frontend/`（src/{pages,components,stores,services/api,utils,types,features}）、`backend/`（src/modules/{auth,reminders,...}）、`docs/`（需求/方案/计划/交接/审查 五件套）、`tools/scripts/`（开发与验证脚本）
- 最小可运行：见 §2

## 1. 给 AI 的三大约定（开工第一课）
1. **文档先行且同步**：需求/方案/计划/交接文档是"活文档"——每完成一个功能，把变更同步进对应文档（本项目 4 份 docs 必须同批提交）。
2. **遵循既有模式**：新实体要注册到 `app.module.ts`；写库用 `repo.update({id,userId},patch)` 而非 `save(entity)`；所有新实体**不声明匿名 @Index**；上传/媒体经 `filesApi` 统一处理。
3. **改完必验证**：`npm run build` ×2 + 单测 + E2E + 关键流程冒烟；涉及视觉的补截图（capture-ui）并跑 UI 评审。

## 2. 快速启动（布谷实例）
```powershell
# 一键（推荐）：自动清端口→启后端(3000)→启前端(5173)→开浏览器
powershell -ExecutionPolicy Bypass -File tools/scripts/dev-all.ps1
# 手动
cd backend  && npm i && cp .env.example .env && npm run start:dev   # http://localhost:3000/api
cd frontend && npm i && npm run dev                                  # http://localhost:5173
# 测试
cd backend  && npm run build && npx jest --runInBand
cd frontend && npm run build && npx vitest run && npm run test:e2e
```

## 3. 已知深坑速查（来自《开发经验汇总》— 完整列表见 E:\ASEE\项目\开发\开发经验）
| # | 坑 | 一句话对策 |
|---|---|---|
| A1 | PS5.1 中文乱码 | 脚本 UTF-8 + BOM、全角标点转 ASCII |
| B1-B5 | TypeORM/SQLite | 显式 varchar、transformer、`update()` 非 save、无匿名 @Index、删库重建 |
| C1 | E2E 限流 429 | 共享账号 + `E2E_RATE_LIMIT` 豁免（仅测试） |
| D1 | 图片"不显示" | vite 代理要含静态目录（/uploads） |
| D2 | 状态不同步 | 状态源多处（互动表/计数/实体）必须同步删除/更新 |
| F | 公开 GitHub | 发布前执行 `docs/···` 与 `E:\ASEE\项目\开发\本地信息\README.md` §2 审计四连 |

## 4. 交付清单（每个里程碑做一遍）
1. 功能完成 + `npm run build` 双端 0 error
2. 单测（新增逻辑必有）+ E2E（旅程级）+ 冒烟（API 级，脚本存 tools/scripts/smoke-*.mjs）
3. docs 五件套同步 + CHANGELOG 更新
4. 视觉变更 → `capture-ui.mjs` 截图 → 多模态评审 ≥ 8（评分表入 docs/UI评审报告.md）
5. 提交规范：Conventional Commits（`feat/fix/docs/test/chore`，中文一行摘要 + 一行要点）
6. 发布前：`E:\ASEE\项目\开发\本地信息\README.md` §2 审计四连 + 敏感物不入库

## 5. 版权与安全（本项目沉淀）
- 代码文件头 `@Sdrop 布谷(Cuckoo) v1 SKEY_<base64>` 水印；校验 `node tools/scripts/sdrop-verify.mjs`；
  水印格式/解码算法/盖章清单见 `E:\ASEE\项目\开发\水印信息\README.md`
- 本地敏感信息（.env、数据库、测试账号）备份在 `E:\ASEE\项目\开发\本地信息\`，**严禁入库**
- 新项目沿用同一套目录结构即可（水印项目名替换为你的项目名）

---
*开始.md 由布谷项目迭代总结沉淀（2026-08-29）；后续项目在此模板上追加特有约定即可。*
